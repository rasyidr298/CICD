on:
  push:
    branches:
      - main
name: Build
jobs:
  test:
    name: Build
    runs-on: macOS-latest
    strategy:
        matrix:
          destination: ['platform=iOS Simulator,OS=14.5,name=iPhone 11', 'platform=tvOS Simulator,OS=13.4,name=Apple TV 4K']
    steps:
      - name: Checkout
        uses: actions/checkout@main
      - name: Build
        run: |
        #   cd Texstyle
          xcodebuild clean build test -project CICD.xcodeproj -scheme CICD -destination "${destination}" CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ONLY_ACTIVE_ARCH=NO
        env:
         destination: ${{ matrix.destination }}
    #   - name: Update Code Coverage
    #     run: |
    #       bash <(curl -s https://codecov.io/bash) -J 'Texstyle'
    #   - name: Update docs
    #     run: |
    #       gem install jazzy
    #       jazzy
    #       git config --local user.email "action@github.com"
    #       git config --local user.name "GitHub Action"
    #       git commit -m "Update docs" -a
    #   - name: Push changes
    #     uses: ad-m/github-push-action@master
    #     with:
    #       github_token: ${{ secrets.GITHUB_TOKEN }}